<div class="row">
    <div class="col-md-6" >
        {{ form_start(form) }}
        <div class="row col-md-5" >
            {{ form_widget(form) }}
            <div class="mt-2">
                <button class="btn btn-success"style="border-radius: 10px; right: 5px"><span style="font-weight: bold">{{ button }}</span></button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>

